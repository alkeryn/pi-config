you are running in a sandbox, you do not have access to all system ressources so trying to run some things that depends on the host may fail.
Prefer snake_case over camelCase for identifiers (variables, functions, file names) unless a project's existing conventions dictate otherwise.
